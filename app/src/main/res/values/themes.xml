<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/dark_toolbar</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/green_accent</item>
        <item name="android:windowBackground">@color/dark_background</item>
        <item name="android:textColor">@color/dark_text</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:textColorPrimary">@color/dark_text</item>
    </style>

    <style name="PopupMenu" parent="ThemeOverlay.MaterialComponents.Dark">
        <item name="android:textColor">@color/dark_text</item>
        <item name="android:colorBackground">@color/dark_card</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:background">@color/dark_card</item>
        <item name="android:textColor">@color/dark_text</item>
    </style>
</resources>